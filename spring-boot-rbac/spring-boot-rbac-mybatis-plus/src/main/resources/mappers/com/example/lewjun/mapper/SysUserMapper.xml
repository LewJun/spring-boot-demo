<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.lewjun.mapper.SysUserMapper">
    <resultMap id="BaseResultMap" type="com.example.lewjun.domain.SysUser">
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="username" jdbcType="VARCHAR" property="username" />
        <result column="email" jdbcType="INTEGER" property="email" />
        <result column="nickname" jdbcType="VARCHAR" property="nickname" />
        <result column="avatar" jdbcType="VARCHAR" property="avatar" />
        <result column="dept_id" javaType="INTEGER" property="deptId" />
    </resultMap>

    <sql id="Base_Column_List">
        id, username, email, nickname, avatar, deptId
    </sql>

    <update id="updateBySelective" parameterType="com.example.lewjun.domain.SysUser">
        update sys_user
        <set>
            <if test="nickname!=null">
                nickname=#{nickname},
            </if>

            <if test="email!=null">
                email=#{email},
            </if>

            <if test="avatar!=null">
                avatar=#{avatar},
            </if>

            <if test="deptId!=null">
                dept_id=#{deptId},
            </if>

        </set>
        where id=#{id}
    </update>
</mapper>